import { theme } from 'antd';

const customTheme = {
  algorithm: theme.darkAlgorithm,
  token: {
    colorPrimary: '#f00',
    borderRadius: 5,
    wireframe: false,
  },
};

export default customTheme;
